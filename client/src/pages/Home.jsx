import SideBar from "@/components/common/SideBar";

const Home = () => {
	return (
		<div className="min-h-screen bg-gray-300 flex">
			<SideBar />
			messagePage
		</div>
	);
};

export default Home;
